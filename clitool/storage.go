package clitool

const fileName = "tasks.json"

func LoadTasks() []Task {
	// open file, decode JSON, return slice
	return []Task{}
}

func SaveTasks(tasks []Task) {
	// encode slice into JSON and write to file
}
